# HF stack for memorization_detection / MIMIR benchmark.
# transformers 4.5x requires huggingface_hub>=0.34,<1.0 (not 1.x).
# Pin numpy<2 for compatibility with scipy/numexpr stacks on shared clusters.

numpy>=1.26,<2
huggingface_hub>=0.34,<1.0
transformers>=4.36,<5
datasets>=2.19,<4
accelerate>=0.26
regex
requests
tqdm>=4.42
safetensors>=0.4.3
tokenizers>=0.22,<=0.23
hf-xet>=1.1.3,<2
sentencepiece
dill
multiprocess
filelock
pyyaml
# infilling_score / memorization_detection imports sklearn.metrics
scipy>=1.10,<2
scikit-learn>=1.3,<2
